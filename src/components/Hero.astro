---
import { useTranslations, type Lang } from '../i18n/utils';
import SkyShader from './SkyShader.astro';
import HeroMarquee from './HeroMarquee.astro';

interface Props {
  lang?: Lang;
}
const { lang = 'tr' } = Astro.props;
const t = useTranslations(lang);
---

<section class="hero-sky relative -mt-[72px] min-h-screen flex items-center justify-center overflow-hidden">
  <SkyShader />
  <div class="relative z-10 max-w-7xl mx-auto px-gutter md:px-margin text-center py-28">
    <h1 class="font-headline text-4xl sm:text-5xl md:text-[64px] leading-tight text-on-surface mb-6 max-w-4xl mx-auto font-bold tracking-tight">
      {t('hero.titleA')} <br />
      <span class="text-secondary relative">
        {t('hero.titleB')}
        <svg class="absolute -bottom-2 left-0 w-full h-3 text-secondary-container opacity-50" fill="none" viewBox="0 0 200 9" xmlns="http://www.w3.org/2000/svg">
          <path d="M2.00021 6.94586C32.0645 2.85921 95.8454 -1.74542 198 6.94586" stroke="currentColor" stroke-linecap="round" stroke-width="3"></path>
        </svg>
      </span>
    </h1>
  </div>
  <HeroMarquee lang={lang} />
</section>

<style>
  /* shader öncesi fallback gradient. */
  .hero-sky {
    background: linear-gradient(to bottom, #3f86d6 0%, #7fb4ee 30%, #c2def9 52%, #eef5fc 66%, #fcf8f8 80%);
  }
</style>
